import React from 'react';

import MainNavigation from './../Navigation/MainNavigation';

const Header = (props) => {
    return ( 
        <header>
        <h1>Hikeouts</h1>
        <MainNavigation />
        </header>
     );
}
 
export default Header;